apiVersion: v1
kind: Pod
metadata:
  name: example-resource-pod
spec:
  containers:
  - name: resource-container
    image: nginx
    resources:
      requests:
        memory: "1Gi"
        cpu: "1"
      limits:
        memory: "2Gi"
        cpu: "2"
